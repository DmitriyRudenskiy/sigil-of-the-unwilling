Ниже представлен подробный чеклист задач, составленный на основе предоставленного аудита проекта «Sigil of the Unwilling». Задачи сгруппированы по приоритетам и направлениям для удобства планирования спринтов или работы локального агента.

---

# 📋 Чеклист задач по результатам аудита

## 🔴 1. Критические задачи и Безопасность (Приоритет: High)
*Эти задачи необходимо выполнить в первую очередь для обеспечения безопасности и стабильности.*

- [x] **MCP-сервер: Аутентификация.** Добавить проверку токена (`MCP_AUTH_TOKEN` через env-переменную) в `_handle_command` (`mcp_interaction_server.gd`).
- [x] **MCP-сервер: Песочница для кода.** Ограничить выполнение произвольного кода в `_cmd_eval` и `_cmd_script` через черный список паттернов (запрет `OS.execute`, `FileAccess.open`, `load` и т.д.) в `mcp_commands_system.gd`.
- [x] **Сохранения: Резервные копии.** Добавить автосоздание `.bak` файла при сохранении в `SaveManager.gd` для возможности восстановления при повреждении основного файла.
- [x] **MCP-сервер: Валидация путей.** Реализовать проверку путей по белому списку в `_cmd_change_scene` для предотвращения загрузки произвольных сцен.
- [x] **MCP-сервер: Обработка ошибок сети.** Добавить проверку `HTTPRequest.RESULT_TIMEOUT` в `_cmd_http_request` (`mcp_commands_network.gd`). _(закрыто удалением: `_cmd_http_request` и вся сетевая группа убраны как неиспользуемые)_
- [x] **MCP-сервер: Null-safety.** Добавить проверку `if instance == null` после `packed.instantiate()` в `_cmd_instantiate_scene`.
- [x] **MCP-сервер: Конфигурация.** Вынести хардкод порта `9090` из `mcp_interaction_server.gd` в `ProjectSettings` или переменные окружения.

## 🟠 2. Технический долг и Архитектура (Приоритет: High/Medium)
*Рефакторинг для улучшения поддерживаемости, устранения антипаттернов и нарушения SOLID.*

- [ ] **HeroController: Устранение скрытой связности (R10).** Удалить аксессоры (`get`/`set`) и заменить обращения во всех файлах (например, `hero.movement` $\to$ `hero.movement_comp.get_controller()`).
- [x] **DRY: Базовые проверки (R6).** Вынести повторяющуюся проверку `is_inside_tree` в метод `_require_scene_tree()` в `McpCommandsBase` и обновить все MCP-команды.
- [ ] **DRY: Поиск нод.** Вынести проверку `if node == null or not node is X` в хелпер `_get_node_of_type()` в `mcp_commands_render.gd`.
- [ ] **SRP: WorldEventRouter.** Рефакторинг `setup()`: убрать 15 параметров, внедрить передачу контекстного объекта (Data Class) или разбить класс на более узкие роутеры.
- [ ] **SRP: BattleController.** Разделить монолитный класс на отдельные компоненты: управление состоянием, ввод, рендер и UI.
- [ ] **DI: Уход от Service Locator.** Заменить `Services.resolve()` на инъекцию зависимостей, особенно в тестовых фабриках (`tests/helpers/factories.gd`).
- [x] **Надежность: Инварианты (R9).** Добавить `assert()` для проверки инвариантов (например, отсутствие мертвых юнитов в очереди) в `BattleState.gd` (`place_army`, `build_queue`, `advance_turn`).

## 🟡 3. Тестовая инфраструктура: Фундаментальные исправления (Приоритет: High)
*Исправление структурных проблем, мешающих стабильной работе CI/CD.*

- [x] **Конфликт имен.** Переименовать `tests/helpers/test_factories.gd` в `test_factory_usage.gd` (устранение конфликта с `class_name TestFactories`).
- [ ] **Утечки памяти.** Добавить `auto_free()` во все тесты, создающие `Node`-объекты.
- [x] **Инициализация классов.** Заменить `load("res://...").new()` на прямое использование `class_name.new()` (затрагивает ~15 файлов, включая `test_battle_state.gd`, `test_map_model.gd`).
- [ ] **Автозагрузки в тестах.** Добавить фолбэк-инициализацию `Units` и `Resources` в тестах, где они используются напрямую.
- [ ] **Базовый класс тестов.** Создать `tests/helpers/base_test.gd` с утилитами (`ensure_units`, `make_battle_state_safe`).
- [ ] **Типизированные ассерты.** Массово заменить `assert_that()` на `assert_int()`, `assert_str()`, `assert_bool()` и т.д. для улучшения читаемости.
- [x] **DRY в тестах.** Вынести дублирующийся сетап (например, `_setup_flying_vs_ground`, `_make_city`) в `TestFactories`.
- [x] **Структура папок.** Перенести содержимое `tests/core/` в `tests/unit/core/` для унификации.

## 🔵 4. Масштабируемость и Читаемость (Приоритет: Medium)
*Улучшение организации кода для облегчения онбординга и навигации.*

- [x] **GameNumbers.gd (R4).** Разбить монолитный файл на доменные: `GameNumbersBattle.gd`, `GameNumbersCity.gd`, `GameNumbersMap.gd` и т.д.
- [ ] **ThemeConfig.gd.** Аналогично разбить константы по доменам/смысловым блокам.
- [ ] **Нейминг.** Переименовать неочевидные аббревиатуры `_grp_input` $\to$ `_input_commands` (и аналогичные) в `mcp_interaction_server.gd`.
- [ ] **Магические числа.** Добавить комментарии, объясняющие происхождение чисел (например, `BATTLE_BOARD_W = 17`) в `GameNumbers.gd`.
- [ ] **Длинные функции.** Разбить `_cmd_environment` и `_cmd_render_settings` в `mcp_commands_render.gd` на подфункции по группам свойств.
- [ ] **Структура GameText.gd.** Сгруппировать однострочные функции по доменам с комментариями-разделителями.

## 🟢 5. Алгоритмы и Оптимизация (Приоритет: Low/Medium)
*Улучшение производительности и защита от крашей на больших данных.*

- [x] **Дейкстра.** Добавить раннее прерывание в `HexPathfinding.dijkstra()`, когда минимальный элемент в куче превышает `max_cost`.
- [ ] **Кэширование.** Оптимизировать `BattleState._cache_signature()`: использовать `hash()` от ключей вместо конкатенации строк.
- [ ] **Аллокации.** Переиспользовать словарь с `.clear()` вместо создания нового `new_visible` в `VisibilityMap.recompute`.
- [x] **Обход дерева (R8).** Заменить рекурсию на итеративный обход со стеком в `McpCommandsBase._find_by_class_recursive()` (защита от переполнения стека на глубоких деревьях).
- [ ] **Документация алгоритмов.** Добавить комментарий в `HexPathfinding.bfs_reachable()` про неочевидное поведение при `steps = 0`.

## ⚪ 6. Очистка мертвого кода (Приоритет: Low)
- [x] **Удаление методов (R1).** Удалить `McpCommandsBase.is_async()` (всегда возвращает `false`).
- [x] **Удаление 3D команд (R7).** Удалить неиспользуемые 3D-команды (`_cmd_csg`, `_cmd_multimesh`, `_cmd_light_3d` и др.) из `mcp_commands_render.gd` (проект 2D).
- [x] **Сетевой код.** Пометить как экспериментальные или удалить неиспользуемые фичи (WebSocket, RPC) из `McpCommandsNetwork`, если они не применяются в проде.

## 📝 7. Написание новых тестов (Покрытие пробелов)

### Unit & Integration Tests
- [x] **HeroController:** Тесты компонентной системы, сериализации, `end_turn`.
- [x] **TemplateEngine:** Тесты всех шаблонов заклинаний и обработки `unknown_template`.
- [x] **WorldBootstrap:** Тест создания всех систем без краша (`test_world_bootstrap.gd`).
- [x] **BattleController:** Прямые тесты контроллера (без эмулятора).
- [x] **SaveManager:** Тесты `delete_slot`, `has_save_in_slot`.
- [x] **Chronicle:** Тесты генерации текста (`_entry_text`).
- [x] **Автозагрузки:** Тесты `SoundManager` (null стрим) и `Settings` (битый файл).
- [ ] **Граничные случаи (Edge-cases):** Дополнить существующие тесты (`MinHeap` с дубликатами, `HexPathfinding` с `INF`-стоимостью, `VisibilityMap` с мусором, миграция сейвов `v6->v7`).

### MCP Tests (Python E2E)
- [x] **Character Creation:** Полный цикл создания персонажа через меню.
- [x] **Hero Death:** Смерть героя и проверка механики преемственности.
- [x] **Battle Controller:** Запуск и проверка боя через реальный контроллер сцены.
- [x] **Endgame:** Проверка условий победы и поражения.
- [x] **Settings Menu:** Применение настроек через UI.
- [ ] **Исправление текущих E2E:**
  - [ ] `test_battle_full_e2e.py`: Добавить жесткий `assert state["over"] == True`.
  - [ ] `test_city_cycle.py`: Добавить проверку существования героя и города.
  - [ ] `test_save_load_continue.py`: Добавить проверку имени, позиции и ресурсов после загрузки.

---
**💡 Рекомендация по порядку выполнения:**
1. Неделя 1: **Безопасность (Раздел 1)** + **Фундамент тестов (Раздел 3)**.
2. Неделя 2: **Технический долг (Раздел 2)** + **Написание недостающих Unit-тестов (Раздел 7)**.
3. Неделя 3: **Масштабируемость (Раздел 4)** + **E2E MCP тесты (Раздел 7)**.
4. Неделя 4: **Оптимизация (Раздел 5)** + **Очистка (Раздел 6)**.