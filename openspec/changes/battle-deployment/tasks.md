## 1. Размещение (BattleState)

- [x] 1.1 В `_build_units` заменить формулу: колонка `col = 0 if is_atk else BW - 1`, ряды `row = i` (одна линия вплотную, порядок стэков).
- [x] 1.2 Добавить защитный обход занятых/препятствий: если клетка колонки занята — занять следующую свободную в той же колонке (`_cell_taken`).

## 2. Проверка

- [x] 2.1 Прогнать `game/tools/shell/run_operability.sh`; вердикт `CLEAN` (0 ошибок, 0 предупреждений).
- [x] 2.2 Размещение одну линию у края подтверждено юнит-тестами (headless без скриншотов): `_test_deployment_line_at_edge`, `_test_deployment_max_capacity`.
- [x] 2.3 Краевые гексы `x=0`/`x=BW-1` не режутся — ок, сдвиг не нужен.

## 3. Тесты

- [x] 3.1 Добавить юнит-тест размещения в `game/tests/`: одна колонка, край, без разрывов, порядок стэков, обход препятствия. (3 новых теста, все проходят.)

## 4. Документация

- [x] 4.1 Описание размещения задокуировано в `openspec/changes/battle-deployment/design.md`; целевой `game/docs/battle_spells.md` отсутствует (удалён), отдельная правка не требуется.

## 5. Коммит

- [x] 5.1 Закоммитить только файлы текущего цикла (`battle-deployment`), с актом по задачам (e08d1ef).
