# Tasks: city-in-world

- [x] 1. `City`: `owner: StringName = &"none"` — serialize/deserialize (аддитивно)
- [x] 2. `City.deserialize`: восстанавливать `display_name` (дефект: serialize содержит, deserialize игнорирует)
- [x] 3. `City.garrison_count() -> int` (`count_state(MILITIA)`) — производный гарнизон
- [x] 4. `CityManager`: `set_tile_yield_provider` хранить Callable + применять в `register_city` (фикс: новые города без провайдера); добавить `city_at(cell) -> City`
- [x] 5. `CityYieldTable.gd` (`scripts/world/`, static): `yield_for_terrain(terrain_id) -> Dictionary` FIDSI по таблице из design p.3
- [x] 6. `WorldBootstrap`: реальный yield-провайдер через `map_gen.get_terrain_id`; `capital.owner = &"player"`
- [x] 7. `CityBalance`: `ROYALTY_FRACTION`, `VILLAGE_START_WORKERS/INDUSTRY/GOLD/FOOD`
- [x] 8. `CityFactory.gd` (`scripts/world/`, static): `create_village(center, name, seed)` (owner player, storage/food/gold, workers на соседей центра), `village_name(seed, cell)`
- [x] 9. `WorldInteractionController.capture_village_at` -> `bool`
- [x] 10. `WorldEventRouter._on_village`: create+register_city (CityFactory) при успешном захвате; `_on_hero_moved` + `_on_village`: `open_city_screen` (идемпотентно)
- [x] 11. `CityIncomeProcessor.gd` (`scripts/economy/`): phase `&"city_income"`, priority 15; дань `floor(gold * ROYALTY_FRACTION)` с `resource_ctx`; отчёт `{total, per_city}`; регистрация в `WorldBootstrap`
- [x] 12. `WorldEventRouter._on_end_turn`: после `execute_turn` — `hero.add_strategic_resource` по `total` дани + `GameLogger`
- [x] 13. `Follower.gd` (`scripts/entities/`, RefCounted): uid/name/race/path/trait_ids + serialize/deserialize
- [x] 14. `FollowerSystem.gd` (static): `make_follower`, `recruit(city, hero, rng)` — убрать FOLLOWER-pop, имя + черты из registry, `hero.followers.append`
- [x] 15. `HeroController`: `followers: Array` + serialize/deserialize (аддитивно)
- [x] 16. `CityScreen.gd` (`scripts/ui/`, Node2D): переезд из CityArenaView (гексы/кликеры/палитра/tooltip/HUD/камера), `setup(city, cfg)` (palette/actions/top_hud_fn/tooltip_fn/ring_color_fn/build_fn/upgrade_hero_cell/feature+clusters opt), `open()/close()`
- [x] 17. `CityArenaView`: тонкий хост CityScreen (cfg = CityArenaModel); песочница (turn/auto/starve/score) без изменений; `CityArena.tscn` не трогать
- [x] 18. `WorldUIManager`: создание CityScreen (код, hidden; headless — нет), `open_city_screen/close_city_screen/city_overlay_open`; cfg мира (палитра, «Нанять»/«Уровень»/«Закрыть», tooltip по `tile_yield_fn`); full-rect mouse swallow
- [x] 19. Input guard: `WorldShortcuts` (оверлей: только Esc) и `WorldInput` (движение заблокировано)
- [x] 20. `WorldPersistence._restore_cities`: ре-крейт захваченных из `world_delta.captured_villages` (CityFactory + register_city) до матча по uid; имя от `(run_seed, cell)`
- [x] 21. Тесты: `test_city_yield_table`, `test_city_income_processor`, `test_city_persistence`, `test_follower`, `test_city_screen` (plan в design p.9)
- [x] 22. Интеграция: сценарий захват → экран → постройка → end turn → gold в UI; save/load round-trip (uid, owner, display_name, followers)
