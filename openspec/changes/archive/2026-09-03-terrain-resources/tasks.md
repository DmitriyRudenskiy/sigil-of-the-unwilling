## 0. Уточнение решений с пользователем
### 0.1
Собрать ответы по Open Question цикла.
- [x] Добыча: из соседней клетки (горы непроходимы) или гора проходима для добычи?
- [x] Истощение: навсегда / возобновляемо / по лимиту?
- [x] Плотность: все клетки или выборочно (параметр)?
- [x] Требование: юнит «worker»/навык или базовая добыча всегда?
- [x] Охват: только лес/гора, или расширить на снег/болото?
- [x] Визуал: формат маркировки.

## 1. Движок «террейн → ресурс»
### 1.1
Создать `TerrainResourceMap` (напр. `game/scripts/data/TerrainResourceMap.gd`).
- [x] `FOREST → wood`, `MOUNTAIN → stone`
- [x] `get_resource_id(terrain_id) -> StringName` (пусто, если нет сопоставления)

## 2. Размещение точек добычи
### 2.1
Добавить поле `terrain_resource_cells: Dictionary` в `MapModel`.
### 2.2
Реализовать `MapSpawner.place_terrain_resources()` (плотность по решению 0.1).
- [x] Проходить `terrain_grid`, помечать FOREST/MOUNTAIN по плотности
- [x] Не накладываться на `resource_cells`/village/enemy
### 2.3
Вызвать `place_terrain_resources()` в `MapGenerator.generate()`.

## 3. Добыча по контакту
### 3.1
В `WorldEventRouter._on_hero_moved(cell)` проверить точку добычи.
- [x] Если клетка в `terrain_resource_cells` и не истощена → начислить `wood`/`stone`
- [x] Начисление через `hero.resources` (+ `resources_changed`)
- [x] Эмитить сигнал добычи / лог
### 3.2
Обрабатывать добычу из соседней клетки для гор (вопрос 0.1), если выбрано.

## 4. Истощение + персистентность
### 4.1
Помечать истощённые точки (D4).
- [x] Убирать клетку из активных точек / вести сет истощённых
### 4.2
Персистентность через `world_delta`.
- [x] `add_removed_terrain_resource(cell)` + восстановление при загрузке
- [x] Сериализация в состоянии мира

## 5. Визуальная маркировка
### 5.1
Маркировать точки добычи и выработанные клетки (D5).
- [x] Спрайт/иконка над клеткой; выработанная — без иконки/замещённая
- [x] Через существующие слои рендера/спавнера

## 6. Проверка и коммит
### 6.1
Запустить `bash game/tools/shell/run_operability.sh`, убедиться в вердикте CLEAN.
- [x] Исправить SCRIPT ERROR/Parse/Run ошибки
- [x] При необходимости обновить `docs/CONSOLE_ALLOWLIST.md`

### 6.2
Добавить автотесты: генерация точек, добыча wood/stone, истощение.
- [x] Точки появляются на лесу/горе
- [x] Добыча начисляет ресурс и истощает точку
- [x] Старые `resource_cells` работают как раньше

### 6.3
Комит только файлов цикла `terrain-resources`.
- [x] `MapModel`, `MapSpawner`, `MapGenerator`, `WorldEventRouter`, `WorldDelta`,
      `TerrainResourceMap`, рендер (по необходимости)
- [x] Не выносить файлы других циклов
