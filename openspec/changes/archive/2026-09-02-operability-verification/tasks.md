## 1. Инвентаризация точек входа и маркеров

- [x] 1.1 Перечислить все сцены (`game/scenes/MainMenu.tscn`, `CityArena.tscn`, `World.tscn`, `Battle.tscn`) и для каждой определить каноничный SceneTree-запуск (`_init()` + `quit()`), гарантирующий self-termination.
- [x] 1.2 Перечислить авто-сценарии (`scenario_{1..7}`) и способ их запуска через `play_scenario.sh`.
- [x] 1.3 Определить юнит-тесты как прогон `run_tests.gd` через `run_all_ci_checks.sh --tests`.
- [x] 1.4 Зафиксировать таксономию маркеров (ошибки/предупреждения) и путь к allowlist (`docs/CONSOLE_ALLOWLIST.md`) — переиспользовать из `check_console_clean.sh`.

## 2. Runner полной проверки (`game/tools/shell/run_operability.sh`)

- [x] 2.1 Реализовать обёртку запуска Godot через `run_godot` (sleep + kill, без `timeout`) с индивидуальным таймаутом на точку входа.
- [x] 2.2 Авто-загрузка реестра `global_script_class_cache.cfg` (как в `run_all_ci_checks.sh`), если его нет.
- [x] 2.3 Запуск сцен через SceneTree-скрипты (`-s`), сценариев через `play_scenario.sh`, тестов через `run_all_ci_checks.sh --tests`; захват лога в `/tmp/operability_sc*.log`.
- [x] 2.4 Вызов `check_console_clean.sh` для console-clean проверки сценариев 1–5 (переиспользование, без дублирования маркеров).
- [x] 2.5 Собрать вердикт «чисто/грязно» и записать `/tmp/operability_report.md` (точки входа, находки, итог).

## 3. Run-and-fix цикл на шаблонах скила

- [x] 3.1 Реализовать цикл run → capture → scan → fix → verify: на любой находке (ошибка или предупреждение вне allowlist) применять шаблоны исправления из `godot-run-and-fix` и повторять точку входа.
- [x] 3.2 Ограничить число итераций (например, до 5) с фиксацией, если чистота не достигнута, — не зацикливаться.
- [x] 3.3 В отчёт записывать, какие находки были исправлены и какими шаблонами.

## 4. Проверка end-to-end и чистота вердикта

- [x] 4.1 Прогонить `run_operability.sh` целиком и убедиться, что все точки входа отработали без зависаний.
- [x] 4.2 Довести консоль до чистого состояния (исправить найденные `SCRIPT ERROR` / runtime-ошибки/предупреждения вне allowlist) и зафиксировать вердикт «clean».
- [x] 4.3 Убедиться, что прогал не рвётся из-за ложных срабатываний (в маркерах провала только Godot-префиксы, не внутренние логи приложения).

## 5. Интеграция в документацию

- [x] 5.1 Добавить ссылку на `run_operability.sh` и скил `godot-run-and-fix` в `AGENT.md` (как на процедуру полной проверки работоспособности).
- [x] 5.2 Зафиксировать в `docs/` (howto), как запустить полную проверку и читать `/tmp/operability_report.md`.
