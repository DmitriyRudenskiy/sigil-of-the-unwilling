## 0. Уточнение решений с пользователем
### 0.1
Собрать ответы по Open Question цикла.
- [ ] Добыча: из соседней клетки (горы непроходимы) или гора проходима для добычи?
- [ ] Истощение: навсегда / возобновляемо / по лимиту?
- [ ] Плотность: все клетки или выборочно (параметр)?
- [ ] Требование: юнит «worker»/навык или базовая добыча всегда?
- [ ] Охват: только лес/гора, или расширить на снег/болото?
- [ ] Визуал: формат маркировки.

## 1. Движок «террейн → ресурс»
### 1.1
Создать `TerrainResourceMap` (напр. `game/scripts/data/TerrainResourceMap.gd`).
- [ ] `FOREST → wood`, `MOUNTAIN → stone`
- [ ] `get_resource_id(terrain_id) -> StringName` (пусто, если нет сопоставления)

## 2. Размещение точек добычи
### 2.1
Добавить поле `terrain_resource_cells: Dictionary` в `MapModel`.
### 2.2
Реализовать `MapSpawner.place_terrain_resources()` (плотность по решению 0.1).
- [ ] Проходить `terrain_grid`, помечать FOREST/MOUNTAIN по плотности
- [ ] Не накладываться на `resource_cells`/village/enemy
### 2.3
Вызвать `place_terrain_resources()` в `MapGenerator.generate()`.

## 3. Добыча по контакту
### 3.1
В `WorldEventRouter._on_hero_moved(cell)` проверить точку добычи.
- [ ] Если клетка в `terrain_resource_cells` и не истощена → начислить `wood`/`stone`
- [ ] Начисление через `hero.resources` (+ `resources_changed`)
- [ ] Эмитить сигнал добычи / лог
### 3.2
Обрабатывать добычу из соседней клетки для гор (вопрос 0.1), если выбрано.

## 4. Истощение + персистентность
### 4.1
Помечать истощённые точки (D4).
- [ ] Убирать клетку из активных точек / вести сет истощённых
### 4.2
Персистентность через `world_delta`.
- [ ] `add_removed_terrain_resource(cell)` + восстановление при загрузке
- [ ] Сериализация в состоянии мира

## 5. Визуальная маркировка
### 5.1
Маркировать точки добычи и выработанные клетки (D5).
- [ ] Спрайт/иконка над клеткой; выработанная — без иконки/замещённая
- [ ] Через существующие слои рендера/спавнера

## 6. Проверка и коммит
### 6.1
Запустить `bash game/tools/shell/run_operability.sh`, убедиться в вердикте CLEAN.
- [ ] Исправить SCRIPT ERROR/Parse/Run ошибки
- [ ] При необходимости обновить `docs/CONSOLE_ALLOWLIST.md`

### 6.2
Добавить автотесты: генерация точек, добыча wood/stone, истощение.
- [ ] Точки появляются на лесу/горе
- [ ] Добыча начисляет ресурс и истощает точку
- [ ] Старые `resource_cells` работают как раньше

### 6.3
Комит только файлов цикла `terrain-resources`.
- [ ] `MapModel`, `MapSpawner`, `MapGenerator`, `WorldEventRouter`, `WorldDelta`,
      `TerrainResourceMap`, рендер (по необходимости)
- [ ] Не выносить файлы других циклов
