# Tasks: Аудит документации и тестов

## 1. Снимок и подготовка

- [x] 1.1 Зафиксировать снимок состояния: `git status --short` (категории и
      количество), `git log -1 --oneline`, дата — сохранить для метаданных отчёта
- [x] 1.2 Подготовить скрипты-помощники в `tmp/audit/` (gitignored): извлечение
      md-ссылок/`res://`-путей/пути-к-файлам из доков; сбор справочников кода
      (autoload из `project.godot`, `class_name` из `game/scripts/`, ключи настроек)
- [x] 1.3 Создать скелет отчёта `docs/misc/audit-docs-tests-2026-08-31.md`
      по структуре design D4 (резюме, 2 блока, таблица findings, приложение)

## 2. Аудит документации: ссылки и соответствие коду

- [x] 2.1 Прогнать ссылочный скан по `docs/` + `AGENT.md` + `README.md` +
      `game/README.md`: md-ссылки, `res://`-пути, пути к файлам/скриптам/сценам —
      резолв против рабочего дерева (design D2/D3); собрать кандидатов с мёртвыми
      целями
- [x] 2.2 Сверить имена autoload и `class_name` из доков со справочником кода
      (`project.godot`, `game/scripts/`); собрать кандидатов с несуществующими
      именами
- [x] 2.3 Вручную подтвердить/отбросить каждого кандидата из 2.1–2.2
      (ложные срабатывания → приложение отчёта, подтверждённые → findings)

## 3. Аудит документации: индекс и покрытие

- [x] 3.1 Сверить `docs/README.md` с фактическим деревом: проиндексированы ли все
      `.md`, нет ли сирот (файл без записи) и дублей (запись без файла)
- [x] 3.2 Составить матрицу «система → документ»: системы из `AGENT.md` +
      подпапки `game/scripts/` + main-спеки + активные changes → есть ли документ;
      пустые строки → findings по P0/P1 (design D4)
- [x] 3.3 Спот-чек содержания 5 ключевых систем (turn pipeline, battle,
      city/economy, magic, world): 3–5 фактов на систему против кода
      (design D2); устаревшие факты → findings P1
- [x] 3.4 Найти дубли и противоречия между документами (одна тема в нескольких
      файлах / разные описания одного поведения) → findings P1/P2

## 4. Аудит тестов: исполняемость и «осиротевшие»

- [x] 4.1 Headless-запуск `run_tests.gd --list` (фоновый процесс + sleep/kill,
      без `timeout`); зафиксировать фактический набор подобраемых тест-методов
- [x] 4.2 Diff «файлы на диске `test_*.gd` + `fakes/`» против набора из 4.1:
      не подбираемые файлы/методы (именование, skip-список раннера) → findings
      P0 с указанием причины
- [x] 4.3 Составить матрицу «система → тесты» по именованию/импортам тестов;
      системы без тестов → findings (P0 для критичных: save/load, battle state,
      turn pipeline; P1 для остальных)

## 5. Аудит тестов: качество и CI

- [x] 5.1 Эвристика слабых ассертов: тест-методы без assert/expect,
      `assert(true)`, print-only; вручную подтвердить кандидатов → findings P1
- [x] 5.2 Инвентаризация асинхронных тестов (`await`/таймеры): флаги flakiness,
      зависимость от wall-clock; проверить, попадают ли они в CI-прогон →
      findings P1
- [x] 5.3 Сверить CI-шаги с покрытием: каждый шаг `run_all_ci_checks.sh`
      (compile_all, check_scene_refs, spell_validation, check_tileset, unit_tests)
      — закрывает ли свой заявленный инвариант; дубли покрытия между тестами →
      findings P2, пробелы → P0/P1

## 6. Отчёт

- [x] 6.1 Заполнить таблицу findings (ID `F-<n>`, приоритет P0–P2, область,
      finding с файлом:строкой или командой воспроизведения, рекомендация);
      резюме с количеством по приоритетам
- [x] 6.2 Приложение: методология (D2), сырые данные сканов, отброшенные ложные
      срабатывания, снимок `git status` (1.1)
- [x] 6.3 Запись в индекс `docs/README.md` (область `misc`, строка-описание) —
      единственная правка существующего файла (design D5)

## 7. Финальная проверка

- [x] 7.1 Прогнать ссылочный скан (2.1) на сам отчёт: все внутренние/внешние
      ссылки отчёта резолвятся
- [x] 7.2 `openspec validate audit-docs-and-tests` — валиден
- [x] 7.3 Git-гигиена: в коммит попадают только отчёт + строка индекса +
      артефакты change; dirty-состояние пользователя (418 D, untracked)
      не тронуто; `tmp/audit/` не закоммичен
- [x] 7.4 Отметить задачи, показать итог: количество findings по P0/P1/P2,
      рекомендация по следующему change на исправления
